ELF64 AARCH64

HEADER
    magic 7f ELF
    class 64
    endian little
    version current
    machine EM_AARCH64
    type ET_EXEC
    entry _start

SECTIONS
    .text executable
    .rodata readable
    .data readable writable
    .bss readable writable

ENTRY
    _start
